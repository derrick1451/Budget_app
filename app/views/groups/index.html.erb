<div class='container'>
<div class='header'>
<h1>CATEGORIES</h1>
</div>
<div class='cat-container'>
<% @groups.each do |group| %>
 <%= link_to group_payments_path(group) do %>
<div class='category'  >
  <%= image_tag(group.icon, alt: 'Icon') %>
  <div class='products'>
<h2><%=group.name%></h2>
<p><%=group.created_at.to_date%></p>
</div>
<p>$<%=group.toal_amount%></p>
</div>
<%end%>
<%end%>
</div>
</div>
<div class='btn-div'>
<%=link_to 'Add New Category',new_group_path,class:'category-btn'%>
</div>